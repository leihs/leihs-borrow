#!/usr/bin/env bash
set -euxo pipefail

PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
source ${PROJECT_DIR}/shared-clj/bin/helpers/clj.sh
# DEV_INIT_EXPR="(require 'leihs.borrow.dev)"
PROJECT_NAME=borrow clj_run --dev-mode true --repl true run "$@"

# vi: ft=sh
