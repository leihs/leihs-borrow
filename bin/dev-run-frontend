#!/bin/bash -eu

PROJECT_DIR="$(dirname ${BASH_SOURCE})/.."
cd "${PROJECT_DIR}"
echo "PWD: $PWD"
source $PROJECT_DIR/shared-clj/clojure/bin/activate

# UI: try to get from cache, otherwise do a FULL REBUILD!
./leihs-ui/bin/build || ./leihs-ui/bin/build-core

./bin/cljs-watch
cd -

# vi: ft=sh
