#!/usr/bin/env bash

set -eux

if [ -z $1 ]; then
  echo "You must provide a git ref."
  exit 1
fi

cd spec/support/legacy-api
git fetch
git switch -d --recurse-submodules $1
cd -
git add spec/support/legacy-api
git commit -m 'update legacy-api'
git push
