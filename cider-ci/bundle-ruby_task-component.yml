traits:
  asdf-ruby: true
  Bash: true

scripts:
  bundle-ruby:
    exclusive_executor_resource: asdf-ruby
    timeout: 20 Minutes
    body: |
      #!/usr/bin/env bash
      set -eux
      cd $LEIHS_BORROW_DIR
      asdf install ruby
      bundle install

