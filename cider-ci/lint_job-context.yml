contexts:
  graphql:
    include: cider-ci/graph-ql-check_context_component.yml

  clojure:
    task_defaults:
      max_trials: 1

    tasks:
      lint-clojure:
        name: Lint Clojure and Clojurescript (NOT ENFORCED YET)
        scripts:
          main:
            body: |
              ./bin/lint-clojure

              # ignore errors (for now)
              exit 0
