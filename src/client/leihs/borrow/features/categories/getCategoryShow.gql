query getCategoryShow(
  $categoryId: UUID!,
  $parentId: UUID,
  $afterCursor: String,
  $userId: UUID
) {
  category(id: $categoryId, parentId: $parentId) {
    id
    name
    children(userId: $userId) {
      id
      name
    }
  }
}
