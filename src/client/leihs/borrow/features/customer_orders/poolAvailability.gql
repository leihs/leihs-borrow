query poolsAvailability($ids: [UUID!], $startDate: Date, $endDate: Date, $includeDates: Boolean!, $includeAltDates: Boolean!) {
  inventoryPools(ids: $ids) {
    id
    maximumReservationDuration
    reservationAdvanceDays
    transferBufferBeforePickUp
    holidays @include(if: $includeDates) {
      startDate
      endDate
      name
    }
    availability(startDate: $startDate, endDate: $endDate) @include(if: $includeDates) {
      dates {
        date
        startDateRestrictions
        endDateRestrictions
      }
      datesForAltLocations @include(if: $includeAltDates) {
        date
        startDateRestrictions
        endDateRestrictions
      }
    }
  }
}
