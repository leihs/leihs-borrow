query getPool($id: UUID!) {
  inventoryPool(id: $id) {
    id
    description
    email
    name
    shortname
    hasReservableItems
    maximumReservationDuration
    holidays {
      startDate
      endDate
      name
    }
    workdays {
      day
      open
      info
    }
  }
}
