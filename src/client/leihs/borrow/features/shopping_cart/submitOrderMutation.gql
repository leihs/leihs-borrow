mutation submitOrder(
  $purpose: NonEmptyText!,
  $title: NonEmptyText!,
  $userId: UUID
) {
   submitOrder(
    purpose: $purpose,
    title: $title,
    userId: $userId
  ) {
    id
    fromDate
    untilDate
    totalDays
  }
}
